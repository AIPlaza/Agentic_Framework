# Logical framework (logframe) prompt template

A partir del brief del proyecto, genera un marco lógico en la estructura Impact -> Outcomes -> Outputs -> Activities. Devuelve un JSON con las claves: impact, outcomes[], outputs[], activities[]

Cada outcome y output debe incluir un ejemplo de indicador (nombre y unidad) en un campo sample_indicator.
